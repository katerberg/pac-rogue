/**
 * Presentation metadata (no Phaser types).
 * Render reads id/color/radius to sync GameObjects; Position stays authoritative.
 */
export const Drawable = {
  id: [] as string[],
  color: [] as number[],
  radius: [] as number[],
};
